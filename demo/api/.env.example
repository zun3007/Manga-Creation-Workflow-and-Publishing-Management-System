# ── Server ──
PORT=3000
CLIENT_URL=http://localhost:5173

# ── Database (matches demo/docker-compose.yml) ──
DB_HOST=127.0.0.1
DB_PORT=3307
DB_USER=root
DB_PASSWORD=inkframe_root
DB_NAME=manga_creation_workflow_and_publishing_management_system

# ── JWT ──
JWT_SECRET=change-me-in-real-projects-inkframe-demo
JWT_EXPIRES=1d

# ── Demo login account (password is (re)applied on API boot) ──
DEMO_USER_EMAIL=dungminer69@gmail.com
DEMO_USER_PASSWORD=Dung123456@

# ── Google OAuth (fill these in — see demo/README.md) ──
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:3000/api/auth/google/callback
